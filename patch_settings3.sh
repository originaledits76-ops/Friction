sed -i 's/val context = LocalContext.current/val context = LocalContext.current\n    var showAppClassification by remember { mutableStateOf(false) }/g' app/src/main/java/com/example/features/settings/SettingsScreen.kt
sed -i 's/            if (rules.isNotEmpty()) {/            item {\n                Button(\n                    onClick = { showAppClassification = true },\n                    modifier = Modifier\n                        .fillMaxWidth()\n                        .height(56.dp),\n                    shape = RoundedCornerShape(16.dp),\n                    colors = ButtonDefaults.buttonColors(containerColor = DarkSurface, contentColor = TextPrimary)\n                ) {\n                    Icon(Icons.Default.AppRegistration, contentDescription = "App Classification")\n                    Spacer(modifier = Modifier.width(8.dp))\n                    Text("App Classification", fontWeight = FontWeight.Bold, fontSize = 16.sp)\n                }\n            }\n\n            if (rules.isNotEmpty()) {/g' app/src/main/java/com/example/features/settings/SettingsScreen.kt
